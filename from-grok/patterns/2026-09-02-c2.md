# C2 — Зигзаг A-B-C AND направление 4h

```
Статус: Knowledge Candidate. Не Foundation. Не PREF. Не канон.
Дата: 2026-09-02
Слой: AND(EWP zigzag P1, HTF 4h body). Не SMC. Не объём. Не новый учебник.
```

## Зачем

C1 (объём) WR не поднял. Дыра №1 в `2026-09-02-wr60-kontekst.md`: паттерн 1h без направления старшего ТФ. 4h бар собирается из того же K-0 1h (resample), новый zip не нужен.

## Слои

| Код | Слой | Роль |
|---|---|---|
| P1 | EWP zigzag 1h | структура, resume = против C |
| HTF | последний **полностью закрытый** 4h бар с концом ≤ t | фильтр направления |

## if/then (ЗАМОРОЖЕН до прогона C2)

```
IF сработал P1 zigzag_ABC на 1h баре t
   AND direction_resume = противоположна C
   AND 4h бар строится как floor(hour UTC / 4)*4 (группы 00-03, 04-07, 08-11, 12-15, 16-19, 20-23)
   AND берётся последний 4h, у которого время закрытия ≤ t (не текущий незакрытый)
   AND sign(close_4h − open_4h) = direction_resume
      (doji close=open → фильтра нет, C2 не сигнал)
THEN сигнал C2 на баре t
   AND диагностика как у P1: target 0.382×|C|, stop 0.236×|C|, H=48 баров 1h
ELSE нет C2
```

Пороги P1 не менять. После WR C2 не тюнить знак 4h. Не live.

## Метрики

n_C2, WR, сколько P1 отрезал 4h, expectancy в стопах, сравнение с P1 40.6% и C1 39.1%. n<20 → INSUFFICIENT_SAMPLE.

## Статус

Заморожен до прогона. Демо по C2 нет, пока нет n≥20 и WR≥60%.
