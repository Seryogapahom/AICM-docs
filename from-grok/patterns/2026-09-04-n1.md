# N1 — P1 AND «4h не пилит» (mid-range)

Статус: Knowledge Candidate. Не Foundation. Не канон.
Дата заморозки: 2026-09-04
Слой: схема распознавания P1 + положение close на 4h. Не C2 (там знак тела 4h). Не учебник Frost.

## Зачем

W1 (будни) не лифт (40.31%). Дыра wr60 #2: отсечь пилу на старшем ТФ не знаком свечи, а дистанцией close от mid диапазона. C2 уже провален — не повторять.

## if/then (ЗАМОРОЖЕН до прогона)

```
IF сработал P1 zigzag_ABC на 1h баре t
AND есть 20 полностью закрытых 4h баров с open_time_end ≤ t
AND range = max(high) − min(low) по этим 20
AND mid = (max(high) + min(low)) / 2
AND range > 0
AND abs(close_последнего_4h − mid) / range ≥ τ
   τ = 0.25 (заморожен)
THEN сигнал N1 на баре t
   диагностика как P1: target 0.382×|C|, stop 0.236×|C|, H=48
ELSE нет N1
```

Пороги P1 и τ **не** крутить после WR. C1–C3/S1/H1/S2/I1/W1 не ретро-tune. Не live.

## Метрики

n_N1, WR, сколько P1 отрезал «пил» (corr mid), expectancy, vs P1 40.60%. n<20 → INSUFFICIENT_SAMPLE.
