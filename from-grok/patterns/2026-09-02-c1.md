# C1 — Зигзаг A-B-C AND объём-CONFIRM (комбо слоёв)

```
Статус: Knowledge Candidate. Не Foundation. Не PREF. Не канон AICM.
Дата: 2026-09-02
Слой: AND(EWP zigzag P1, объём CONFIRM P4). Не новый учебник. Не SMC. Не флаг.
```

## Зачем

Соло P1 WR 40.6% на K-0 (n=1574). Владелец 2026-09-02: комбинировать Эллиотт с другими правилами, чтобы тянуть диагностический WR к ≥60%. Объём остаётся CONFIRM, не вход.

## Слои (имена не сливать)

| Код | Слой | Карточка | Роль в C1 |
|---|---|---|---|
| P1 | EWP zigzag A-B-C | `2026-09-02-p1.md` | структура коррекции, направление resume = против C |
| P4 | объём CONFIRM | `2026-09-02-p4.md` | фильтр: импульс resume на объёме > SMA20 и откат на слабом объёме |

Provenance P1: Waveopedia zigzags / ChartSchool / дайджест Frost-Prechter.  
Provenance P4: пакет Геворкяна (Hedliners), не волны.

## if/then (ЗАМОРОЖЕН до прогона C1)

```
IF сработал P1 zigzag_ABC на баре t (confirm пивота C)
   AND direction_resume = противоположна направлению C
   AND существует окно k ∈ {t .. t+3}:
         на баре k (или i в [k-3..k] с pullback j) выполнен P4 CONFIRM_CONTINUE
         с direction = direction_resume
THEN сигнал C1 на баре confirm P4 внутри окна
   AND диагностика как у P1: target = 0.382×|C|, stop = 0.236×|C|, H=48 баров 1h от сигнала C1
ELSE нет сигнала C1
   (P1-соло при этом не становится входом)
```

Параметры P1 и P4 **не** менять. Не D-1. Не live. После WR C1 пороги не тюнить.

## Метрики отчёта (обязательно)

- n_C1, wins, WR
- n_P1 (базовая 1574 / 40.6% на том же ряде, если ряд тот же)
- сколько P1 отрезал P4 (атрибуция фильтра)
- при n<20: INSUFFICIENT_SAMPLE
- expectancy в единицах стопа (wins×0.382/0.236 − losses×1), рядом с WR, не вместо

## Статус

**Кандидат комбо.** Прогон ещё не делали. Демо-вход по C1 запрещён, пока нет n≥20 и WR не известен.
