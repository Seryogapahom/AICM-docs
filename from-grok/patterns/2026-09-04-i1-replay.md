# I1 replay — инвалидация счёта (пробой старта A до цели)

Статус прогона: **SUCCESS**  
Диагностика, не P&L. Не Foundation. Не канон. Не live. Не D-1 (R=0.10 не использован).  
Git кладёт штаб. Спека FROZEN **не** редактировалась. Пороги P1 и правило пробоя старта A после WR **не** тюнились.  
S2 **не** пересчитывался (уже сдан: n=670 WR 38.81%). C1–C3/H1 **не** пересчитывались.

Дата прогона: **2026-09-04** (Europe/Zurich).  
Спецификация (FROZEN): `from-grok/patterns/2026-09-04-i1.md`  
Детектор: локальная копия P1 (`detect_zigzags` + `outcome` + константы) из `(локальный артефакт прогона, не в git)` с записью `price_start_A = a0["price"]` **без** смены порогов.  
Локальный скрипт (не в репо): `(локальный артефакт прогона, не в git)`  
Числа **только из этого прогона**.

## Ряд / окно

- CSV: `(локальный артефакт прогона, не в git)`
- window start: `2017-08-17T04:00:00Z`
- window end: `2026-07-31T23:00:00Z`
- **n_bars:** `78373`
- n_pivots (фрактал L=3): `12453`

Окно совпало с K-0 / P1 day-1. Прогон не остановлен.

## Замороженные параметры (не тюнились после WR)

- PIVOT_L = 3
- ZZ_B ∈ [0.382, 0.886]
- |C| ≥ |A| (ZZ_C_GE_A = 1.0)
- B не за стартом A
- target = 0.382×|C|, stop = 0.236×|C|, H = 48 баров 1h
- entry = close бара t (confirm C); смотреть с t+1; stop first if both (как P1)
- direction_resume: bull_zz → −1; bear_zz → +1
- D-1 R=0.10 **не использован**

## Операционализация INVALID (HQ brief)

- bull_zz (структура L-H-L-H, C вверх): `price_start_A = a0["price"]` (начальный L); INVALID если на любом баре окна **low < start_A** до разрешения stop/target/H.
- bear_zz (структура H-L-H-L, C вниз): `price_start_A = a0["price"]` (начальный H); INVALID если на любом баре окна **high > start_A** до разрешения stop/target/H.
- На одном баре: INVALID проверяется **раньше** stop/target → INVALID побеждает.
- INVALID **не** win и **не** lose; в `n_live` / `WR_live` **не** входит (исключается, не считается убытком).
- Иначе исход как у P1 (target / stop / timeout по close vs entry).

## Проверка P1-соло (этот прогон)

Ожидание day-1: n=1574, wins=639, WR≈40.60%.

| Метрика | Значение |
|---|---|
| n_P1 | **1574** |
| wins_P1 | **639** |
| losses_P1 | 935 |
| WR_P1 | 0.405972045743 (**40.60%**) |
| outcome None (пропущены) | 0 |
| совпало с day-1 | **да** |

## I1 (диагностика)

| Метрика | Значение |
|---|---|
| n_P1 | 1574 |
| **n_INVALID** | **196** |
| **n_live** (= n_P1 − n_INVALID) | **1378** |
| wins_live | 498 |
| losses_live | 880 |
| **WR_live** | 0.361393323657 (**36.14%**) |
| expectancy_sum (wins×0.382/0.236 − losses×1) | -73.9153 |
| expectancy_mean / live | -0.0536 |
| статус | **OK** |

INVALID ≠ lose: из 196 INVALID раньше были P1-win=141, P1-lose=55; все исключены из live, не записаны как losses.

### По видам ZZ

| kind | n_P1 | n_INVALID | n_live | wins_live |
|---|---|---|---|---|
| bull_zz | 823 | 109 | 714 | 257 |
| bear_zz | 751 | 87 | 664 | 241 |

## vs P1 40.60% n=1574

| | WR | n |
|---|---|---|
| P1 solo (этот прогон) | **40.60%** | 1574 |
| I1 WR_live | **36.14%** | 1378 |
| Δ WR (live − P1) | -4.46 п.п. | — |

S2 (не пересчитывался): n=670 WR 38.81%.

## Находка

Счета с пробоем старта A до цели выкинуты как INVALID (n_INVALID=196). На живых n_live=1378 WR_live=36.14%. Пороги и правило пробоя **не** крутить после этого WR. Не live.

## Артефакты

- JSON: `(локальный артефакт прогона, не в git)`
- MD: `(локальный артефакт прогона, не в git)`
- CSV сигналов (не git): `(локальный артефакт прогона, не в git)`
- summary tmp: `(локальный артефакт прогона, не в git)`

## Запреты соблюдены

- пороги P1 не ретюнились; правило break-of-start-A не менялось после WR
- frozen spec не редактировался
- git не add/commit/push; `(локальный артефакт прогона, не в git)` не писался
- не live; D-1 R=0.10 не использован
- S2 не пересчитывался
